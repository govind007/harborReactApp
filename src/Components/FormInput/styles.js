import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  inputError: {
    borderColor: '#dc3545',
  },
  input: {
    borderColor: '#c0cbd3',
  },
  errorText: {
    color: '#dc3545',
    marginBottom: 20,
  },
})

export default styles
